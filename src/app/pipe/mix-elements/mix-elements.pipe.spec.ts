import { MixElementsPipe } from './mix-elements.pipe';

describe('MixElementsPipe', () => {
  it('create an instance', () => {
    const pipe = new MixElementsPipe();
    expect(pipe).toBeTruthy();
  });
});
